import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app smoke test', (WidgetTester tester) async {
    // Placeholder — full integration tests require a real database
    expect(true, isTrue);
  });
}
